# Frontend runtime config for TrustMe.
# mock = run entirely on local seed data (safe for demos)
# live = talk to the backend REST API at /api
VITE_API_MODE=mock

# Optional: where the dev-server proxy forwards /api in live mode.
VITE_API_TARGET=http://localhost:8000
